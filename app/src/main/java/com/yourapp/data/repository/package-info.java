package com.yourapp.data.repository;

/**
 * Repository pattern package - abstraction over data sources.
 * Contains: AuthRepository, UserRepository, MessageRepository, ConversationRepository
 * Acts as single source of truth, combining local and remote data
 */
