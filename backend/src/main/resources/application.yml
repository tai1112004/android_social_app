spring:
  application:
    name: social-messaging-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/social_app}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:01112004}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  upload-dir: ${UPLOAD_DIR:uploads}

cloudinary:
  cloud-name: du9ru66mw
  api-key: 453815286563816
  api-secret: Je_J0fopIm7s7v-WDsZ0HJYjfwA
  folder: social-app

server:
  port: 8082

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-secret-key-must-be-at-least-32-characters-long}
  expiration:
    access: 900000        # 15 minutes in milliseconds
    refresh: 604800000    # 7 days in milliseconds

logging:
  level:
    root: INFO
    com.yourapp: DEBUG
